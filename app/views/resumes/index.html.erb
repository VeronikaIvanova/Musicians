<p id="notice"><%= notice %></p>

<h1>Listing Resumes</h1>

<table>
  <thead>
    <tr>
      <th>Instrumentalist</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @resumes.each do |resume| %>
      <tr>
        <td><%= Instrument.find(Instrumentalist.find(resume.instrumentalist_id).instrument_id).name %></td>
        <td> <%= link_to "#{User.find(Instrumentalist.find(resume.instrumentalist_id).user_id).firstname}
            #{User.find(Instrumentalist.find(resume.instrumentalist_id).user_id).lastname}", 
            "/users/#{current_user.id}" %> | </td>
        <td><%= link_to 'Show', resume %> </td>
        <% if User.find(Instrumentalist.find(resume.instrumentalist_id).user_id).id==current_user.id %>
          <td>|<%= link_to 'Edit', edit_resume_path(resume) %> </td>
          <td>|<%= link_to 'Destroy', resume, method: :delete, data: { confirm: 'Are you sure?' } %> </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Resume', new_resume_path %>
