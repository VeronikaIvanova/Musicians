<%= form_for [@group, @group_note] do |f| %>
  <div class="field">
     <%= f.text_area :text, placeholder: "Compose text..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>