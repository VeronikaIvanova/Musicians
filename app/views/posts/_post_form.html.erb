<%= bootstrap_form_for(@user_note) do |f| %>

  <div class="field">
     <%= f.text_area :text, placeholder: "Compose text..." %> <br>
  
  <%= f.submit "Post", class: "btn btn-large btn-primary" %></div>
<% end %>