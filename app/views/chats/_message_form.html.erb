<%= form_for [@chat, @message]  do |f| %>
    <%= f.text_area :text, placeholder: "Compose new message...", class: "form-control input-lg", id: "inputlg"%>

  <%= f.submit "Send" , class: "btn btn-large btn-primary" %>
<% end %>
