 <div id="follow_form">
  <% if ConcertFollower.where(user_id: current_user.id, concert_id: @concert.id).first!=nil %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>